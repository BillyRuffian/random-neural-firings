.header.header-fixed.u-unselectable
  .header-brand
    .nav-item.no-hover
      %h6.title RNF
    .nav-item
      %span.icon.text-blue-400
        %i.fa-solid.fa-dice-d20
    #header-btn.nav-item.nav-btn
      %span
      %span
      %span
  #header-menu.header-nav
    .nav-right
      - if user_signed_in?
        .nav-item 
          = link_to new_note_path, data: { 'turbo-frame' => 'notes-container' } do
            %i.pr-1.fa-wrapper.fa-solid.fa-plus
            Note
        .nav-item
          = @user.email
        .nav-item
          = link_to destroy_user_session_path, data: { turbo_method: :delete } do 
            %i.fa-solid.fa-arrow-right-from-bracket
